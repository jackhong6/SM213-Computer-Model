ldi0:ld $0x0, r0  #load 0x0 into r0
ldi1:ld $0x1, r1  #load 0x1 into r1
ldi2:ld $0x2, r2  #load 0x2 into r2
ldi3:ld $0x3, r3  #load 0x3 into r3
ldi4:ld $0x4, r4  #load 0x4 into r4
ldi7:ld $0x7, r7  #load 0x7 into r7
halt

ldbo: ld 4(r0), r1 #load mem[8] into r1

ldbi:
ld (r0,r4,4), r2 #load mem[20] into r2

ldin:

stbo:

stin:

rrmv:

add:

and:

inc:

inca:

dec:

deca:



halt